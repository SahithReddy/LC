/*
Given a non-empty string s, you may delete at most one character. Judge whether you can make it a palindrome.

Example 1:
Input: "aba"
Output: True
Example 2:
Input: "abca"
Output: True
Explanation: You could delete the character 'c'.

*/

class Solution {
    public boolean validPalindrome(String s) {
        int b=0; 
        int e = s.length()-1;
        while(b<=e){
           if(s.charAt(b)==s.charAt(e)){
            b++;
            e--;
            }
            else return isPalindrome(s, b+1, e) || isPalindrome(s,b, e-1);
        } return true;
    }
    
    public boolean isPalindrome(String s, int b, int e){
        while(b<=e){
            if(s.charAt(b)==s.charAt(e)){
            b++;
            e--;
            }
            else return false;
        } return true;
    }
}

// Time Complexity: O(N)O(N) where NN is the length of the string. Each of two checks of whether some substring is a palindrome is O(N)O(N).

// Space Complexity: O(1)O(1) additional complexity. Only pointers were stored in memory.
